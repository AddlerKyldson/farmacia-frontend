import React from "react";
import Layout from "../../structure/Layout";

const FormSeries: React.FC = () => {
    return (
        <Layout>
            <h1>{"Formulário Series"}</h1>
        </Layout>
    );
};

export default FormSeries;
