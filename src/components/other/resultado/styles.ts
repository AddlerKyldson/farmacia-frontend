import styled from "styled-components";

export const ContainerResultado = styled.div`
    background-color: #fff;
    padding: 1rem;
    margin-top: 1rem;
    border: solid 1px #eee;
    border-radius: 0.5rem;
    position: relative;
`;

export const ResultadoTitle = styled.h2`
    font-size: 1.5rem;
    margin-top: 0;
`;

export const ContainerTableResultado = styled.div`
    display: flex;
    flex-direction: column;
`;
